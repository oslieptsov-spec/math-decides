# Forbidden strings. Case-insensitive, one ERE pattern per line.
# Checked against: staged diff (pre-commit), working tree, full git history,
# file names, and the commit author address.
#
# OWNER: extend with the internal vocabulary — method names, server paths,
# internal file names, hostnames, namespaces, config names.

# --- organisation and contacts ---
saa[- ]?alliance
@saa-alliance\.com
\bSAA\b

# --- internal vocabulary (seed, extend) ---
fitted[- ]control
\bV10\b
батарейн
battery doctrine

# --- secrets ---
nvapi-[A-Za-z0-9_-]{8,}
AIza[0-9A-Za-z_-]{30,}
-----BEGIN [A-Z ]*PRIVATE KEY-----

# --- session infrastructure: names and addresses of the machine the H100 run
# --- happened on. Nothing published should carry them; the sweep is what makes
# --- that a guarantee rather than a habit.
189\.1\.169\.61
brev-[a-z0-9]{6,}
shadeform
arin22
vf8pof7i2

# --- cluster addresses. A control-plane endpoint or a service address in a
# --- screenshot is the same class of leak as a hostname, and habit is not a
# --- guarantee.
34\.55\.101\.251
axial-entropy-484310-e5
968187468023
