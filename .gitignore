__pycache__/
*.pyc
.venv/
.env
*.key

# Private planning documents — strategy and internal context, never published.
/gtc-berlin-demo.md
.gtc-berlin-demo.v*.bak

# Live counter state. The seed lives in the code; the running total does not
# belong in version control, and it is never reset quietly.
/web/counter.json

# Raw recordings before the manual media sweep.
/media/raw/
